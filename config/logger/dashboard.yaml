# @package logger.loggers.dashboard
_target_: memorax.loggers.DashboardLogger
title: SALT - State-Action Linear Transformer
name: ${algorithm.name}
total_timesteps: ${total_timesteps}
refresh_per_second: 1
env_id: ${environment.env_id}
