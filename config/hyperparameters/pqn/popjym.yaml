# @package _global_

total_timesteps: 15_000_000
num_train_steps: ${eval:${algorithm.num_steps} * ${algorithm.num_envs}}
num_eval_steps: 832

algorithm:
  num_envs: 256
  num_steps: 512
  num_minibatches: 16
  update_epochs: 4
  td_lambda: 0.95
