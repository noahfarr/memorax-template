# @package _global_

total_timesteps: 10_000_000
num_train_steps: ${eval:${algorithm.num_steps} * ${algorithm.num_envs}}

algorithm:
  num_envs: 32
  num_steps: 128
  tau: 1.0
  target_update_frequency: 500
  train_frequency: 4
